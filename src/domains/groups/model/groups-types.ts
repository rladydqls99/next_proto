export interface GroupDto {
  groupCode: string;
  groupName: string;
  description: string;
  memberCount: number;
}

export interface Group {
  groupCode: string;
  groupName: string;
  description: string;
  memberCount: number;
}
