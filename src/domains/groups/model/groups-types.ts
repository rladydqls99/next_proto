export interface GroupDto {
  id: string;
  groupCode: string;
  groupName: string;
  description: string;
  memberCount: number;
}

export interface Group {
  id: string;
  groupCode: string;
  groupName: string;
  description: string;
  memberCount: number;
}
